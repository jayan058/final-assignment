const canvas1 = document.getElementById("canvas1") as HTMLCanvasElement;
const ctx1 = canvas1.getContext("2d") as CanvasRenderingContext2D;

// export function handlePause() {
//   console.log("handlePause called"); // Debugging log
//   let globalAlpha = 0.9; // Set the transparency level (0.0 to 1.0)
//   ctx1.fillStyle = `rgba(100,100, 100,0.01 )`; // Set a yellow color with transparency
//   ctx1.fillRect(0, 0, canvas1.width, canvas1.height);
//   globalAlpha = 1.0; // Reset the transparency to default
// }
